/**
 * Check if an email uses a blocked (personal/free) domain
 */
export declare function isBlockedEmailDomain(email: string): boolean;
/**
 * Extract domain from email address
 */
export declare function getEmailDomain(email: string): string;
